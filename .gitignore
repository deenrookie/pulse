# build artifacts (anchored to the repo root — a bare "pulse" would also
# ignore the cmd/pulse/ source directory)
/pulse.exe
/pulse
*.pdb

# node
web/node_modules/

# runtime data (dev instances)
.pulse-dev/
*.log

# keep the built SPA so fresh clones can `go build` without node
!web/dist/
