// 最小插件骨架：只保留需要的钩子，直接开始改。
plugin = {
  name: "My Plugin",
  version: "1.0",
};

function onRequest(ctx) {
  // ctx.request = { method, url, httpVersion, headers, body }
}

function onResponse(ctx) {
  // ctx.response = { status, reason, httpVersion, headers, body }
}
